---
phase: 03-popup-ui-and-config-actions
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [".planning/ROADMAP.md", ".planning/REQUIREMENTS.md"]
autonomous: true
gap_closure: true
requirements: [POP-02, POP-03]

must_haves:
  truths:
    - "ROADMAP Phase 3 Success Criterion 5 accurately describes the placeholder delivery, not the Phase 4 picker behavior"
    - "REQUIREMENTS.md POP-02 reflects partial Phase 3 delivery (checkbox + delete) with picker trigger completing in Phase 4"
    - "REQUIREMENTS.md POP-03 reflects placeholder delivery in Phase 3 with full implementation in Phase 4"
    - "Traceability table statuses for POP-02 and POP-03 say Partial (Phase 4 completes)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Corrected Phase 3 Success Criterion 5"
      contains: "Coming soon"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated POP-02 and POP-03 with partial delivery notes"
      contains: "Phase 4"
  key_links: []
---

<objective>
Close two documentation gaps from Phase 3 verification: update ROADMAP.md Success Criterion 5 and REQUIREMENTS.md POP-02/POP-03 to reflect the planned placeholder delivery of Add Selector and the Phase 4 deferral of the element picker trigger.

Purpose: Align planning documents with actual Phase 3 delivery so verification passes and Phase 4 planning has accurate upstream context.
Output: Updated ROADMAP.md and REQUIREMENTS.md with accurate Phase 3 scope descriptions.
</objective>

<execution_context>
@/Users/itzik/.claude/get-shit-done/workflows/execute-plan.md
@/Users/itzik/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-popup-ui-and-config-actions/03-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ROADMAP.md Phase 3 Success Criterion 5</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>.planning/ROADMAP.md</read_first>
  <action>
On line 68 of .planning/ROADMAP.md, replace the current Success Criterion 5:

CURRENT (line 68):
```
  5. Clicking the Add Selector (+) button creates a new empty selector row ready for the user to activate the element picker
```

REPLACE WITH:
```
  5. Clicking the Add Selector (+) button shows a "Coming soon" placeholder message — the element picker that creates new selector rows is delivered in Phase 4
```

No other lines change. The plan list on lines 71-73 already correctly describes "Add Selector placeholder" so no update needed there.
  </action>
  <verify>
    <automated>grep -n "Coming soon" .planning/ROADMAP.md | grep -q "Phase 4" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- Line 68 of ROADMAP.md contains: `5. Clicking the Add Selector (+) button shows a "Coming soon" placeholder message — the element picker that creates new selector rows is delivered in Phase 4`
- grep for "creates a new empty selector row ready for the user to activate the element picker" in ROADMAP.md Phase 3 section returns zero matches
- All other ROADMAP.md content is unchanged
  </acceptance_criteria>
  <done>ROADMAP Phase 3 Success Criterion 5 accurately reflects placeholder delivery</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md POP-02 and POP-03 statuses</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md</read_first>
  <action>
Make three changes in .planning/REQUIREMENTS.md:

**Change 1 — POP-02 requirement text (line 38):**

CURRENT:
```
- [x] **POP-02**: Popup lists all configured selectors for the current domain with: enable/disable checkbox, delete button, and element picker trigger per row (FR-201)
```

REPLACE WITH:
```
- [x] **POP-02**: Popup lists all configured selectors for the current domain with: enable/disable checkbox and delete button per row (FR-201). Element picker trigger per row deferred to Phase 4 (PICK-01).
```

**Change 2 — POP-03 requirement text (line 39):**

CURRENT:
```
- [x] **POP-03**: Popup includes an Add Selector button (+) that creates a new empty selector row ready for picker activation (FR-202)
```

REPLACE WITH:
```
- [x] **POP-03**: Popup includes an Add Selector button (+) that shows a "Coming soon" placeholder (FR-202). Full picker activation delivered in Phase 4 (PICK-01).
```

**Change 3 — Traceability table (lines 116-117):**

CURRENT:
```
| POP-02 | Phase 3 | Complete |
| POP-03 | Phase 3 | Complete |
```

REPLACE WITH:
```
| POP-02 | Phase 3 + Phase 4 | Partial (picker trigger in Phase 4) |
| POP-03 | Phase 3 + Phase 4 | Partial (picker activation in Phase 4) |
```
  </action>
  <verify>
    <automated>grep "POP-02" .planning/REQUIREMENTS.md | grep -q "Phase 4" && grep "POP-03" .planning/REQUIREMENTS.md | grep -q "Phase 4" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- POP-02 requirement line contains "Element picker trigger per row deferred to Phase 4 (PICK-01)"
- POP-03 requirement line contains "Coming soon" and "Phase 4 (PICK-01)"
- Traceability table row for POP-02 shows "Phase 3 + Phase 4" and "Partial"
- Traceability table row for POP-03 shows "Phase 3 + Phase 4" and "Partial"
- All other REQUIREMENTS.md content is unchanged
  </acceptance_criteria>
  <done>POP-02 and POP-03 accurately reflect partial Phase 3 delivery with Phase 4 completion</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep "Coming soon" .planning/ROADMAP.md` returns the updated Success Criterion 5
2. `grep "POP-02" .planning/REQUIREMENTS.md` shows Phase 4 deferral note
3. `grep "POP-03" .planning/REQUIREMENTS.md` shows Phase 4 deferral note
4. `grep "Partial" .planning/REQUIREMENTS.md` returns two lines (POP-02 and POP-03 traceability rows)
5. No code files modified — this is documentation only
</verification>

<success_criteria>
- ROADMAP.md Phase 3 Success Criterion 5 matches actual delivery (placeholder, not picker)
- REQUIREMENTS.md POP-02 and POP-03 reflect partial delivery with Phase 4 completion
- Traceability table shows Partial status for both POP-02 and POP-03
- Phase 3 verification gaps are closed without any code changes
</success_criteria>

<output>
After completion, create `.planning/phases/03-popup-ui-and-config-actions/03-03-SUMMARY.md`
</output>
